# @vitejs/plugin-react@4.x only declares peer support up to vite@7, but it runs
# fine on the vite@8 this project uses. Allow npm to resolve past that stale
# peer range instead of aborting the install with ERESOLVE.
legacy-peer-deps=true
